This is mostly a manual process, no need to replicate more than this one time.
ePBRN_computation_reqs.py contains an entrypoint to running a memory profiler for the ePBRN experiment.
FEBRL_computation_reqs.py contains an entrypoint to running a memory profiler for the FEBRL experiment.

To also get the CPU time needs, please run (on linux/mac) `time python <script.py>`